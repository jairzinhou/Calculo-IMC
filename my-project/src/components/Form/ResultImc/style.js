import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({ 
    resultContainer: {
        marginTop: 20,
        alignItems: "center",
      },
      resultText: {
        fontSize: 20,
        fontWeight: "bold",
        color: "#333",
      },
      resultValue: {
        fontSize: 24,
        fontWeight: "bold",
        color: "#4CAF50",
        marginTop: 5,
      },
})

export default styles
