import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    boxTitle: {
      alignItems:"center",
      marginBottom:20
    },
    textTitle:{
        fontWeight:"bold",
        color:"red",
        fontSize:20,
        fontFamily:"Arial"
    }
  });


export default styles